  
# 📘 多阶段多模态图像检索系统技术架构文档

---

# 一、文档概述

## 1.1 文档目的

本文档描述一个**融合爬虫数据获取、向量检索（CLIP）与视觉语言模型（VLM）精排的多阶段多模态图像检索系统**。

系统目标：

- 构建**高召回 + 高精度**的图像检索系统

- 支持复杂语义查询（风格 / 情绪 / 场景）

- 实现**数据自增长能力（爬虫驱动）**

- 支撑工程化部署与项目表达

---

## 1.2 文档范围

涵盖：

- 数据获取（爬虫 / API）

- 多模态向量化（CLIP）

- 向量检索（FAISS / Milvus）

- 多阶段检索（Recall + Re-rank）

- VLM精排

- Query理解（LLM）

- 缓存系统

- API服务

不包含：

- 用户系统

- 分布式调度（预留扩展）

---

## 1.3 核心设计理念

```text

数据驱动 + 多阶段检索 + 智能精排

```

具体为：

```text

爬虫扩充数据 → 向量检索（找得到） → VLM精排（选得准）

```

---

# 二、系统总体架构（重构版）

---

## 2.1 分层架构（关键优化）

```text

┌──────────────────────────────┐

│         用户交互层            │

│ 文本 / 图片 / API            │

└──────────────┬───────────────┘

               ↓

┌──────────────────────────────┐

│       Query理解层（LLM）      │

└──────────────┬───────────────┘

               ↓

═══════════════════════════════════

        🔵 数据层（Data Layer）

═══════════════════════════════════

┌──────────────────────────────┐

│ 爬虫数据获取（在线/离线）      │

│ Bing / Google / API          │

└──────────────┬───────────────┘

               ↓

┌──────────────────────────────┐

│ Embedding（CLIP）            │

└──────────────┬───────────────┘

               ↓

┌──────────────────────────────┐

│ 向量库（FAISS / Milvus）     │

└──────────────────────────────┘

═══════════════════════════════════

        🟡 检索层（Retrieval）

═══════════════════════════════════

┌──────────────────────────────┐

│ 向量召回（Top-K）            │

└──────────────┬───────────────┘

               ↓

┌──────────────────────────────┐

│ 轻量重排（Re-rank）          │

└──────────────┬───────────────┘

═══════════════════════════════════

        🔴 推理层（Reasoning）

═══════════════════════════════════

┌──────────────────────────────┐

│ VLM精排（评分 / 对比）        │

└──────────────┬───────────────┘

               ↓

┌──────────────────────────────┐

│ 结果输出 + API（FastAPI）     │

└──────────────────────────────┘

```

---

## 2.2 架构本质升级

```text

静态图库检索

        ↓

动态数据驱动检索系统

        ↓

多阶段智能检索系统

```

---

# 三、技术栈选型（统一版）

| 技术组件     | 选型方案               | 说明                               |
| ------------ | ---------------------- | ---------------------------------- |
| 向量模型     | CLIP / Chinese-CLIP    | 多模态对齐                         |
| 向量索引     | FAISS                  | 本地高性能                         |
| 向量数据库   | Milvus                 | 可扩展                             |
| 关系数据库   | MySQL                  | 存储图片元数据信息，爬虫任务记录   |
| VLM模型      | GPT-4o / Qwen-VL       | 精排                               |
| 后端框架     | FastAPI                | API服务                            |
| 缓存         | Redis                  | 降成本                             |
| 爬虫         | aiohttp / Playwright   | 数据获取                           |
| 图像处理     | PIL / OpenCV           | 拼图                               |

---

# 四、核心模块设计（完全融合）

---

## 4.1 爬虫数据获取模块（核心新增）

### 模块定位

```text

数据入口（Data Ingestion Layer）

```

---

### 三种模式

#### ✅ 离线采集（推荐基础）

```text

关键词 → 爬虫 → 入库 → 向量化

```

#### ✅ 在线实时爬取

```text

query → 抓图 → 临时候选集 → VLM筛选

```

#### ✅ 混合模式（推荐）

```text

本地库优先 + 爬虫补充

```

---

### 关键能力

- 去重（URL / hash）

- 并发控制（async）

- 图片预过滤（分辨率/质量）

- 反爬策略（UA / proxy）

---

### 与检索系统融合（关键点）

```text

方式1：离线入库 → 向量库

方式2：直接VLM筛选

方式3：临时向量库融合（推荐高级）

```

---

## 4.2 Query理解模块

### 架构

Query  

↓  

Query理解  

↓  

拆分：  

- 向量语义  

- 结构化条件  

- 关键词

### 检索融合

向量检索（CLIP）  

+ BM25（文本）  

+ MySQL过滤（标签/来源）

---

- 解决 CLIP “语义强但可控性差”

- 支持精确过滤（比如：动漫 + 高清 + 横图）

---

LLM → 查询计划（Query Plan）

例如：

```json

{

  "semantic_query": "...",

  "filters": {

    "style": "anime",

    "color": "dark"

  },

  "keywords": ["cyberpunk", "rain"],

  "search_strategy": "hybrid"

}

```

### 检索策略层

```

Query理解

 ↓

策略层（决定怎么搜）

 ↓

执行：

  - 向量检索

  - BM25

  - MySQL

```

## 4.3 Embedding模块

- CLIP向量化（图文统一空间）

- GPU加速

- 向量归一化

---

## 4.4 VectorStore模块

- Top-K召回（100~300）

- 支持多query融合

- 支持临时索引（爬虫数据）

---

## 4.5 多阶段检索模块（核心）

```text

Query

 ↓

Embedding

 ↓

Recall（Top 200）

 ↓

Re-rank（Top 50）

 ↓

VLM（Top 10）

```

---

## 4.6 VLM精排模块（核心）

> [高风险注释] VLM精排属于高成本、易抖动模块，默认仅在候选集收敛后启用。  

> 生产建议：仅对 TopN（建议 10~20）调用，且必须配置超时与降级回退。

### 三种策略

#### ✅ 评分制（推荐）

- 可并行

- 易缓存

#### ✅ Pairwise

- 精度高

- 成本高

#### ⚠️ 淘汰赛（优化建议）

- 保留Top2~3

- 多轮避免误杀

---

### 可靠上线约束（VLM）

- 单请求超时：建议 1.5s~3s（按模型与并发配置）

- 最大重试：1 次（仅幂等场景）

- 失败降级：VLM失败时直接返回 Re-rank 结果

- 熔断策略：连续失败率超过阈值（如 20%）后短时熔断，进入纯向量/轻量重排模式

- 成本闸门：按 query 类别启用（复杂语义开，普通 query 关）

## 4.7 Collage模块

- 拼图供VLM判断

- 自动裁剪主体

- 统一尺寸

---

## 4.8 缓存模块（关键）

| 类型            | 内容                     |
| --------------- | ------------------------ |
| embedding缓存   | query → vector          |
| 检索缓存        | query → ids             |
| VLM缓存         | query+images → result   |
| 爬虫缓存        | query → images          |

---

## 4.9 Metadata模块

```sql

CREATE TABLE images (

    id BIGINT PRIMARY KEY,

    file_path TEXT,

    embedding_id BIGINT,

    metadata JSON,

    created_at TIMESTAMP

);

```

---

## 4.10 Learning-to-Rank（LTR）模块

> [高风险注释] LTR依赖稳定行为数据与偏差校正，早期数据不足时容易引入“伪优化”。  

> 建议作为第三阶段能力：先做日志与评估，不直接接入主链路排序。

1. 先收集弱信号（Weak Signals）

2. 构建排序偏好数据（Pairwise）

3. 用轻量模型学习排序

4. 和 VLM / CLIP 融合

### step 1 数据日志

```

### 收集日志：

query  

image_id  

position  

clicked  

dwell_time  

timestamp

### 存入 MySQL：

user_behavior_log

```

### step 2 构造 Pairwise 数据

```

### 转换规则：

#### ✔ 点击 vs 未点击

clicked = 1 → 正样本  

clicked = 0 → 负样本

---

#### ✔ 停留时间

> 3s → 正样本  

< 1s → 负样本

---

#### ✔ 排序隐式信号（非常关键）

rank 1 clicked vs rank 10 not clicked  

→ 1 > 10

```

### Step 3：训练一个“轻量排序模型”

```

## 推荐模型（工业常用）：

### ✅ LightGBM Ranker（强烈推荐）

或者：

### CatBoost Rank

## 输入特征：

- CLIP similarity  

- position rank  

- image popularity  

- source type  

- click history  

- dwell time

---

## 输出：

ranking score

```

### 新架构

```

CLIP召回（200）

   ↓

LTR排序（轻量过滤）

   ↓

VLM精排（10）

```

### 可靠上线约束（LTR）

- 数据门槛：近30天有效样本达到最低阈值（如 >=10万曝光，>=5000点击）再训练

- 评估门槛：离线 NDCG@10 / MRR 相比基线提升 >=3% 才可灰度

- 灰度门槛：先 5% 流量，验证点击率、停留时长、投诉率

- 偏差处理：显式建模 position bias，避免把展示位误当偏好

- 可回滚：保留无LTR策略开关，支持分钟级回退

### 解决数据少

```

## 🟢 技巧1：用 CLIP similarity 作为“弱标签”

query embedding · image embedding

👉 当作 pseudo label

---

## 🟢 技巧2：用 VLM 自动标注（非常关键）

你可以这样做：

query + image A/B → VLM判断哪个更好

👉 自动生成 pairwise 数据

---

## 🟢 技巧3：用爬虫来源当 bias

Pinterest > random web image  

Pixiv > general scraping

👉 作为 prior feature

---

## 🟢 技巧4：position bias（很重要）

用户点击第1个结果 ≠ 完全真实偏好  

但仍然是强信号

```

# 五、系统流程设计（最终统一）

---

## 5.1 索引构建流程

```text

爬虫 → 图片 → CLIP → 向量 → FAISS → Metadata

```

---

## 5.2 查询流程（完整版）

```text

1. 用户输入

2. Query理解（LLM）

3. 多query生成

4. 查询向量库（Top 200）

5. 判断是否不足：

   → 是：触发爬虫补充

6. 合并候选集

7. Re-rank（Top 50）

8. 拼图

9. VLM精排

10. 返回结果

```

---

# 六、API设计（统一版）

---

## 6.1 搜索接口

```http

POST /search

```

```json

{

  "query": "cyberpunk girl",

  "top_k": 5

}

```

---

## 6.2 爬虫接口

```http

POST /crawl

```

```json

{

  "query": "cyberpunk girl",

  "num": 50

}

```

---

## 6.3 入库接口

```http

POST /ingest/crawl

```

---

# 七、性能与成本设计

---

## 7.1 性能分层

| 层级       | 特点       |
| ---------- | ---------- |
| 向量检索   | ms级       |
| 爬虫       | 网络瓶颈   |
| VLM        | 秒级       |

---

## 7.2 优化策略

- 限制Top-K

- 缓存VLM结果

- 控制爬虫数量（如64）

- 优先本地数据

- 减少拼图轮数

---

# 八、扩展性设计

---

## 8.1 扩展方向

- OCR检索

- Caption生成

- Diffusion生成联动

- Agent自动调用

- 用户反馈学习（RLHF）

---

# 九、项目亮点（最终强化版）

---

## ✅ 技术亮点（可直接写简历）

- 构建**数据自增长的多模态图像检索系统（爬虫 + 向量检索 + VLM）**

- 设计**多阶段检索架构（Recall + Re-rank + 精排）**

- 基于CLIP实现图文跨模态检索

- 引入VLM提升复杂语义理解能力（风格 / 情绪）

- 实现Query语义解析与Multi-Query召回

- 设计缓存系统降低推理成本

- 支持本地图库与实时搜索融合

---

# 十、工程优先级与风险分级（由低到高）

---

> 目标：先做“稳定可用”，再做“复杂智能”。以下顺序用于实际排期与资源投入。

## 10.1 P0（低风险，优先上线）

### 范围

- CLIP + FAISS 基础召回

- Metadata 入库与去重（URL/hash）

- 基础缓存（embedding缓存、检索缓存）

- 基础 API（`/search`、`/crawl`、`/ingest/crawl`）

### 验收标准

- 检索延迟 P95 < 300ms（不含VLM与在线爬虫）

- 服务可用性 >= 99.5%

- 基础离线指标可产出（Recall@K、NDCG@K）

---

## 10.2 P1（中风险，受控增强）

### 范围

- Query 理解与 Multi-Query 召回

- 轻量 Re-rank（不依赖外部大模型）

- 在线爬虫补充（仅补候选，不直接覆盖本地结果）

### 验收标准

- 复杂query命中率相对P0提升（如 Recall@50 提升 >=5%）

- 在线补充命中率可追踪（记录补充命中贡献）

- 故障时可自动降级回 P0 链路

---

## 10.3 P2（高风险，灰度上线）

### 范围

- VLM 精排

- LTR 训练与在线排序融合

### 风险点

- VLM：高成本、高延迟、外部依赖波动

- LTR：数据偏差风险、冷启动噪声、效果不稳定

### 上线策略

- 强制灰度（5%→20%→50%→100%）

- 强制回滚开关（配置化）

- 强制预算闸门（每日调用预算与速率限制）

---

# 十一、可靠方案补充（生产可用）

---

## 11.1 统一超时与降级策略

- Query理解超时：回退原始 query 直搜

- 在线爬虫超时：跳过补充，仅返回本地候选

- VLM超时/失败：回退 Re-rank 结果

- 任一子模块失败：主链路不中断，返回“部分增强结果”

## 11.2 熔断与重试策略

- 外部依赖（VLM、爬虫源）采用熔断器

- 仅幂等读请求允许有限重试（1次）

- 连续失败触发熔断后进入冷却窗口，再半开探测恢复

## 11.3 观测与告警（最小集）

- 指标：QPS、P95/P99延迟、错误率、超时率、缓存命中率、VLM调用量

- 质量：Recall@K、NDCG@K、首屏点击率、零结果率

- 告警：错误率与超时率超过阈值时告警并自动降级

## 11.4 数据治理与合规（最小集）

- 记录图片来源、抓取时间、许可状态、哈希指纹

- 维护来源白名单与下线机制（可按 source 批量移除）

- 敏感/违规内容预过滤，避免进入主索引

## 11.5 版本与回滚机制

- embedding 模型版本、索引版本、排序策略版本全部可追踪

- 新版本上线采用双写/影子评估后切换

- 保留最近稳定版本，支持分钟级回滚